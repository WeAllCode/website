machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  pre:
    - pip install awscli
    - pip install awsebcli
    - docker build -t kand/$CIRCLE_PROJECT_REPONAME:$CIRCLE_SHA1 -f ./resources/docker/Dockerfile .

test:
  override:
    - echo True

deployment:
  elasticbeanstalk:
    branch: feature/docker-circleci
    commands:
      - echo "Work in progress."
      # - ./deploy-docker.sh $CIRCLE_SHA1 coderdojochi
